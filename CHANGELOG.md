# v1.4.8

发布日期：2026-09-19

## 网络与会话

- **冷启动会话探活**：`verifyStoredLogin` 先打 `GET /favorite?page=1`，仅 Authentication 类失败才用凭据走会话恢复，避免夜间误判 401。
- 单请求 `retryTimes` 5 → 1：最坏耗时从约 120s 降到约 40s（覆盖 SDK 默认值）。
- 会话恢复失败冷却改为指数退避（5→10→20→40→80→封顶 120s，成功清零）。
- 域名冷启动只竞速一次后固定；连续 6 次 5xx/IOException 才重新竞速。
- User-Agent 改为设备真实 WebView UA，替换 SDK 硬编码的 Android 9 / Chrome 91。
- JM API 的 GET 请求补 `lang=TW`，对齐官方行为。
- 收藏等同步失败不再立即连环重试，pending 交给下一个 30s 窗口。
- 401 / FormBody 异常日志补充 host、cookie 名与调用栈，便于定位。

## 搜索与标签

- 排序项 `MOST_COLLECT_COUNT` 对齐官方为 `MOST_VIEWED`，文案「最多收藏」→「最多点击」。
- 删除冗余的「首页标签排除」；排除模板已全局覆盖，存量 LocalSetting JSON 无需迁移。

## 体验

- 触感反馈（VIBRATE + AppHaptics）、预测式返回。
- 玻璃组件、设置页与更新检查页细节调整。

## 依赖

- Kotlin 2.4.20，OkHttp 5.5.0（含 `okhttp-urlconnection` / `mockwebserver3` 对齐，避免 4/5 混装崩溃）。
- Compose BOM 2026.09.00、Navigation 2.10.1、Coroutines 1.11.0、Paging 3.5.1。

## 工程与发布

- **编译加速**（不改业务逻辑）：Gradle 配置缓存 / build cache / parallel，堆内存与 KSP Room 增量参数；debug 增量与 release 无变更构建明显变快，R8 冷编译在缓存命中时可复用。
- **发布分支模型**：日常开发在 `canary`；发版时将 `canary` 同步到 `dev` 并推送。`dev` 独有 GitHub Actions CI（官方 Actions **v6**）在推送后构建签名 Release APK，产出挂到 GitHub Release。
- 更新日志与 Release 说明由维护流程在发版时写入 `CHANGELOG.md`，再发布 CI 构建的 APK。

## 安装包

- Release 包名：`kujira.manga`
- 版本号：`1.4.8`（versionCode `148`）


# v1.4.7

发布日期：2026-09-17

## 搜索

- **排除标签只走服务端**：查询串拼 `-tag`，结果返回后不再对每条漫画拉详情做本地二次排除。
  多标签排除时显著降低「有时候特别慢」（旧实现一页最多多打约 20 次详情接口）。
- 关键词与排除项全为空时本地直接失败并提示，不再向后端要「推荐列表」当搜索结果。
- 补齐搜索请求级日志（query / raw / kept / 失败原因），便于区分「关键词没送达」与「本地过滤吃光」。
- 搜索语法说明入口（顶栏「?」玻璃弹窗）。
- 搜索 / 收藏 / 历史 / 周推荐列表统一 Paging：内层 `cachedIn` + 外层 `stateIn(Eagerly)`，
  进详情再返回可复用当前查询缓存，不再整链重建。

## 收藏状态

- **详情页 / 阅读器「已收藏」只认本地收藏快照**（`ObserveLocalFavorite`），不再信云端详情里的
  `is_favorite`。多端互踢、换设备、同步落后时，不再出现「明明收藏了却显示未收藏」。
- 账号切换会重绑观察；未登录视为未收藏。

## 登录与会话

- **会话失效自动恢复单飞 + 冷却**：一批请求同时被踢时只发一次登录；成功约 1.5s 内兄弟请求直接重放，
  失败约 5s 内不再重试，避免连发凭据、后一次登录把刚恢复的会话再踢掉。
- Keystore 暂时不可读时：Cookie / 用户身份用 `getOrNull`，**不再用空列表覆盖完整会话快照**，
  也不把空身份当成「已登出」。
- 「需要登录」错误正确带上 Authentication，收藏相关弹窗能给出登录引导，而不是笼统网络错误。

## 阅读与历史

- **搜索历史**：上限 50 条；懒加载并与磁盘合并去重；写盘异步串行。
- **阅读历史**：Keystore 短暂故障不丢本会话进度；恢复后与磁盘合并（已读章节并集）；写盘异步串行。
- 章节选择弹窗改为玻璃弹窗，与阅读器章节面板同一套 Glass 约束。

## 玻璃 UI

- 提取编码预览、收藏夹管理/移动、封面长按详情、NSFW 提示、自动更新引导、搜索语法说明、
  章节选择等统一落在 `GlassCaptureHost` overlay，拿到真正的毛玻璃而不是静默退化成纯色块。
- **原生模糊失败改为退避重试**（逐步加大间隔、上限 2s）：短暂 GPU 失败后可恢复，
  不再「面板偶尔变成纯色且一直好不了」。
- 封面组件不再自开弹窗，由页面在 overlay 里组合详情面板。
- 移除死路由 `userCollectComic`（收藏已是主 Tab 内嵌）。

## 封面与列表性能

- 滚动（fling）中完成的封面解码结果延迟上屏，停稳后再显示：原先仅首页/收藏，
  现搜索结果、周推荐、历史、评论等分页网格同样生效；内存命中仍立即上屏。
- 封面专用 OkHttp：全局最多 12 并发、每主机 4，与下载/阅读连接池隔离。
- 封面磁盘缓存上限可调（设置 → 其他 →「封面磁盘缓存」，128/256/512/1024 MB），**默认 256 MB**（原先 1 GB）；
  变更后热重建 ImageLoader，无需重启。

## 调色板

- 「莫奈取色」更名为**「自动取色」**（跟随系统壁纸动态色，Android 12+）。
- 调色板列表中「自动取色」左侧显示系统提取出的四色小球（修复此前空白：颜色打包格式错误）。

## 设置

- 「剪切板自动检测」「缓存路径」移至 **隐私** 分区。
- 「强制刷新收藏夹」移至 **连接** 分区。
- 删除「网络推荐」下方说明小字。
- 缓存路径弹窗增加红色提醒：自定义路径可能导致漫画被系统识别为照片而出现在相册中。

## 工程

- 日志页支持**导出日志文件**。
- 缓存目录创建/迁移失败时错误信息带上具体路径，便于排查。
- `ARCHITECTURE.md` 核对至 v1.4.7，补充封面加载、搜索排除、玻璃弹窗与 Paging 共享约定。

## 安装包

- Release 包名：`kujira.manga`
- 版本号：`1.4.7`（versionCode `147`）

# v1.4.6

发布日期：2026-09-15

## 自动检查更新

- 冷启动后静默检查 GitHub Release：若有新版本则弹窗引导，可选「更新」进入检查更新页或「取消」。
- 从后台唤起应用不会触发检查；检查失败不报错、不弹窗。
- 可在 设置 → 检查更新 中关闭「启动时自动检查更新」。

## 平板

- 左侧悬浮导航在滚动（含惯性）时自动隐藏，停止约 500ms 后滑回；不拦截手势。
- 设置一级页双侧留白与悬浮按钮外缘对齐。
- 锚定菜单在内容区坐标系下定位更准确。

## 工程

- `./scripts/android` 增加 `input`（tap/swipe/keyevent）与 `ui-dump`。

## 安装包

- Release 包名：`kujira.manga`
- 版本号：`1.4.6`（versionCode `146`）

# v1.4.5

发布日期：2026-09-15

## 平板模式

- 新增可开关的**平板模式**：设置 → 显示 →「平板模式」。新装且检测为平板时首次询问；手机侧首个非零窗口宽度自动关闭。
- 主 Tab 改为左侧悬浮三枚高斯模糊导航按钮（垂直居中），顶栏与手机一致走玻璃 chrome；首页/收藏下拉「松开搜索」指示器在平板可用。
- 漫画详情：左下阅读、右下四操作两组等宽悬浮玻璃按钮；玻璃弹窗宽度为整屏 3/4。
- 平板自适应网格最小单元格 160dp，缓解横屏快速滑动掉帧。
- 底部提示（Snackbar）在大屏上 640dp 居中，不再铺满整宽。

## 稳定性

- **收藏同步**：多端互踢后 cookie 恢复不再写 username 缓存，避免库内 `login(username, null)` 在 OkHttp FormBody 非空断言崩溃；同类异常归类为「登录已失效」。
- **LazyGrid 重复 key**：收藏 SQL `GROUP BY` + 分页网格唯一 key，消除「Key was already used」闪退。
- **搜索**：查询变化按 revision 换代收集，失败/空结果不再静默展示上一次缓存列表；`cachedIn` 改为挂在每个查询的 Pager 内，避免新旧结果混排。
- **搜索滑到底**：Lazy 页尾指示器去掉 `fillMaxSize`，避免滑到底崩溃。
- **详情页**：移除无用整页下拉刷新（平板上与内嵌评论嵌套滚动会崩）；用户页同样移除。
- **剪贴板检测**：协程取消不再被 `runCatching` 吞掉后误弹「编码无效」。

## 阅读器

- 滚动模式点按翻页改为一次平滑滑动（目标可见时 ease-out-cubic；未进入组合窗口时一次 `animateScrollToItem`），消除闪帧与半屏两段位移。
- 已收藏按钮与详情页一致，使用主题色。

## UI

- 搜索加载骨架与结果列表共用同一套自适应列数，不再固定 3 列。
- 评论区高度改为 `heightIn`，去掉强制 `weight` 撑满；平板详情底部不再出现通栏白框占位。

## 工程

- 删除无调用死代码 `ComicLazyGrid.kt`。
- `ARCHITECTURE.md` 核对数字与平板相关边界更新。

## 安装包

- Release 包名：`kujira.manga`
- 版本号：`1.4.5`（versionCode `145`）

# v1.4.4

发布日期：2026-09-14

## 更名

- 项目更名为 **Kujira-Manga**：仓库名、应用显示名（桌面图标名、关于页、更新页）全部更换。
- 安装包名由 `jmcomic.plus` 改为 `kujira.manga`，debug 变体为 `kujira.manga.debug`。
- 重建了发布密钥库：密钥库由 `release-key/jmcomic-plus-release.p12` 换成
  `release-key/Kujira-Manga-Key.p12`，别名换成 `Kujira-Manga-Key`，密码保存于本机钥匙串
  条目 `Kujira-Manga-Key`。证书与私钥沿用原样（指纹未变），签名身份不变。
- **升级须知**：包名与签名同时变更，系统会把本版视为一个全新的应用。旧版 `jmcomic.plus`
  的设置、登录、收藏与下载数据都不会自动迁移，需要重新登录并按需重新下载。
  旧的 `jmcomic.plus` 可以继续与新版共存，确认新版本可用后再手动卸载。

## 评论与登录

- 修复已登录仍提示 “Please login first” / 无法发表评论的问题：恢复会话时同步写入内置 API 的用户名缓存，评论在服务端成功后不再被客户端误判失败。

## 详情页

- 漫画详情评论区标题行右侧展示上架时间（如「2026年3月6日上架」），与「评论」标题同字体。
- 正确解析上游 Unix 秒/毫秒时间戳，不再显示成一串数字。

## 应用锁

- 从后台切回前台时不再闪现应用内容：离开时更早加锁（`ON_PAUSE`），并禁止系统 recents 截取未锁定界面。
- 截图策略收窄：仅在锁屏界面禁止截图；解锁后可正常截图、录屏。

## 网络

- 修复评论分页隔离与 DoH 客户端初始化竞态相关问题。

## 工程

- 新增统一 Android 调试 CLI `./scripts/android`（安装、logcat、插桩测试等），文档见 `docs/android-cli.md`。
- 清理已关闭的审计报告附件；文档索引见 `docs/README.md`。

## 安装包

- 应用名：`Kujira-Manga`
- Release 包名：`kujira.manga`
- 版本号：`1.4.4`（versionCode `144`）

# v1.4.3

发布日期：2026-09-13

## 安全与备份

- 加密备份格式升级到 v4：不再写入可被离线一次 SHA-256 猜测的密码/图案摘要；凭据正确性只靠慢速派生 + AES-GCM 解密判断。旧 v1–v3 加密备份仍可正常解锁。
- 本地设置写盘改为「写确认后生效」：系统密钥库或磁盘写入失败时，应用锁、隐私开关等不会假装保存成功；桌面图标切换失败会回切到原入口。
- 从备份恢复设置时，若本机已启用应用锁或已完成引导，不会再被备份文件重新打开引导入口，也无法在引导里直接清掉已有锁。
- 备份中的缓存目录/设置段损坏（空字段、空列表元素等）会被明确识别为「内容损坏」，不再崩溃或伪装成空内容恢复成功。

## 阅读与下载

- 阅读页返回键改由系统返回调度统一管理：跳章、打开评论或应用锁后离开阅读器，不会再残留旧页面的返回处理。
- 同组多章节并发完成下载时，缓存索引与数据库状态保持一致；配置文件改为临时文件原子替换，中断写入不会留下半份索引。

## 网络与账号

- 图片 CDN 健康探测与阅读/更新请求共用同一套 DoH 解析，启用 DoH 后探测不再走系统 DNS。
- 阅读历史分页与多选绑定当前登录会话：切换账号、登出或刷新失败会立即清空上一账号的展示与选择，旧选择不会在新账号下提交删除或缓存。

## 其它修复

- 更新下载取消更及时，阻塞读可被外层调用取消打断。
- PDF 导出失败时清理残留临时文件。
- 修复玻璃质感宿主组件重复捕获绘制的问题。

## 安装包

- Release 包名：`jmcomic.plus`
- 版本号：`1.4.3`（versionCode `143`）

# v1.4.2

发布日期：2026-09-10

## 自定义缓存目录（新功能）

- 设置中新增「缓存目录」：可以把漫画下载缓存改到手机上的任意文件夹（通过系统文件选择器），也可以随时切回默认目录。
- 切换目录时会自动迁移已有缓存，迁移成功后才启用新目录；迁移期间原缓存仍然可用，读取失败时整体中止并保留原状态，不会静默丢失已下载内容。

## 下载与缓存

- 迁移时一并搬走暂停或下载中的部分章节，切换目录后恢复下载可接着原有进度，不再从头开始。
- 切换目标目录时只重建将要写入的章节，不会清空目标目录里其它已有内容。
- 「重新下载」后（原文件已删除、记录仍在）切换目录不再直接失败。

## 阅读器

- 阅读过程中应用被系统回收后重新打开，会回到上次阅读的章节。
- 修复阅读页收藏状态更新相关的问题。

## 登录与会话

- 登录过期后可自动恢复会话并重试收藏同步，无需手动重新登录。
- 系统密钥库（Keystore）临时不可用时，不再把空数据写入本地、也不再覆盖已有密文；异常期间不会误显示为「未登录」。

## 网络

- DoH 解析逻辑独立成组件并加强回退，节点异常时切换更可靠。

## 界面与交互

- 应用内确认弹窗统一为玻璃质感样式，与其它界面保持一致。
- 宽屏设备（平板 / 横屏）首页顶部留白与手机一致。
- 开启应用锁时不再丢失主页导航状态。

## 修复

- 修复「图标伪装」实际不生效的问题：此前切换动作作用在不存在的组件上，选择相册 / 系统工具后桌面图标不会变化。
- 优化图标伪装切换顺序：先启用目标入口、再关闭其它入口；若启用失败则保留当前入口，避免桌面图标直接消失，且只有真正切换成功才写入设置。

## 安装包

- Release 包名：`jmcomic.plus`
- 版本号：`1.4.2`（versionCode `142`）

# v1.4.1

发布日期：2026-09-05

## 收藏与同步

- 收藏同步完成后仅在内容、成员关系、顺序或元数据确实变化时刷新收藏页；远端无变化时保留当前列表和滚动位置，避免整页封面闪烁。
- 同步失败时显示具体原因，帮助区分网络异常、登录失效、服务器拒绝和响应解析失败。

## 图片与滚动

- 优化首页和收藏页快速滚动时的漫画封面显示：已加载和内存缓存中的封面继续保持可见，滚动期间新完成的封面延迟到停止后显示，减少掉帧和闪烁。

## 导航与输入

- 搜索页离开时自动清除焦点并收起输入法，系统侧滑返回首页时不再残留键盘。
- 长按页面左上角返回按钮可直接回到首页，减少多层页面逐级返回的操作。

# v1.4.0

发布日期：2026-09-03

## 重要说明

- Android 包名由 `jmcomicoi.net` 改为 `jmcomic.plus`。
- Release APK 改用新的独立发布证书，不再使用 Android 调试证书。
- 由于包名和签名均已更换，系统会把 v1.4.0 识别为新应用；旧版本的登录、设置、收藏缓存和阅读记录不会自动迁移。

## 界面与交互

- 引入统一的玻璃质感导航体系，重做顶栏、底部导航、菜单、弹窗、提示消息和主要页面动效。
- 优化首页分类交互，将原优先级第 2 的推荐页面调整为启动默认首屏。
- 搜索结果切换“最新、最多收藏、最多图片、最多爱心”时立即回到顶部，并记住搜索结果的浏览位置。
- 从漫画详情点击作者或标签进入搜索结果后，返回会直接回到原详情页，不再多经过搜索输入页。
- 改善详情、评论、下载、设置、应用锁、主题配色和启动器伪装的反馈与一致性。

## 阅读器与图片加载

- 重做缩放手势优先级：双指缩放全程独占，不再误触点击、滑动或上下翻页。
- 放大后锁定当前页，单指可按相同速率自由横向、纵向或斜向移动画面；中央双击恢复原始大小。
- 当前页图片增加统一总时限，单页最多尝试两个图片来源，避免网络高峰期长时间竞速和持续加载。
- 当全部节点普遍变慢时自动关闭竞速并顺序尝试，降低无效等待与额外流量。
- 完善图片预加载、请求优先级、去重、解码、缓存、完整图片反混淆、OOM 降级和 CDN 容错。

## 数据、收藏与网络

- 以内置 JMComic API 为主要数据源，升级 API 依赖并加入可配置 DoH 解析。
- 收藏夹升级为 Room 本地优先架构，支持分页、文件夹、搜索筛选、后台同步、强制刷新和下载联动。
- 修复收藏状态、文件夹关系及账号切换过程中的迟到请求、重复同步和并发覆盖问题。
- 优化应用启动、登录/退出、会话持久化、点赞、评论、封面、首页懒加载和网络错误恢复。
- 精简旧数据层与废弃功能，增加阅读器、首页、收藏、导航、设置、会话和兼容性回归测试。

# v1.0.0

一个基本能用的版本，实现了以下功能

- [x] 登录
- [x] 搜索
- [x] 阅读
- [x] 收藏
- [x] 简易用户信息
- [x] 阅读历史
- [x] 漫画详情
- [x] 历史评论
- [x] 漫画评论
- [x] 发表评论
- [x] 首页推荐
- [x] 目录阅读
- [x] 日夜间模式
- [x] 每周必看
- [x] 自动登录

可能会有意想不到的 bug 😫😫，但大部分功能基本可用😘。

支持 android12 以上设备，建议开启梯子进行🛫。
